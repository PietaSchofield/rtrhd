% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cprd_read_schema.R
\name{read_cprd_schema}
\alias{read_cprd_schema}
\title{Read CPRD extract schema (XML) into a tidy map}
\usage{
read_cprd_schema(schema_path)
}
\arguments{
\item{schema_path}{Character scalar. Path to the CPRD XML schema file
(e.g., \code{"path/to/goldcases_Extract_schema.xml"}).}
}
\value{
A named list. Names are recordset names from the schema.
Each element is a tibble with columns:
\describe{
\item{header}{Character; exact column header as it appears in data files.}
\item{pos}{Integer; 1-based column position in the file.}
}
If the file is missing or empty, returns an empty list.
}
\description{
Parses a CPRD \verb{*_Extract_schema.xml} file and returns a named list.
Each list element corresponds to a \verb{<recordset>} and contains a tibble
with the exact column headers and their 1-based positions in-file.
}
\examples{
\dontrun{
  smap <- read_cprd_schema(".../goldcases_Extract_schema.xml")
  names(smap)              # recordset names (e.g., "Therapy", "Test")
  smap$Therapy             # tibble of headers and positions
}

}
